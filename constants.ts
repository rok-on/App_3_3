
import type { User } from './types';

export const USERS: User[] = ['Vid', 'Leonard', 'Mark', 'Tilen'];

export const TASK_ONE_ANSWERS: Record<User, number> = {
  Vid: 4,
  Leonard: 1,
  Mark: 3,
  Tilen: 2,
};

export const TASK_TWO_ROUNDS = 30;
